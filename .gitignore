# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
.turbo
.next
out
dist
build
*.tsbuildinfo

# Expo
.expo
.expo-shared
ios
android

# Convex
.convex

# Env files (broad — F3 fix in KIT_RETROSPECTIVE.md)
.env*
!.env.example

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Boot-gate scratch
.boot-gate-logs/

# Local user scratch (caught .env.steve etc that bit us)
*.local

# Convex AI files install side effects (KIT_RETROSPECTIVE P7).
# `npx convex ai-files install` run from repo root in a monorepo creates
# .agents/ and .claude/skills/ with broken pointers. Skills belong at
# user-global ~/.claude/skills/, not in-repo.
.agents/
.claude/skills/
